// import {routers,appRouter} from '@/router/router';

const app = {
    state: {
      menuTheme: 'dark', // 主题
      openedSubmenuArr: [], // 要展开的菜单数组
      menuList: [],
    },
    mutations: {

    }
};

export default app;
